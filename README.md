# package creation

create a package

1. flutter create --template=package your_package_name
2.  export package name your_package
example : library;

export 'your widget';

3. Use in main app
dependencies:
  package_name:
    path: package path

4. use ui page 
import filepath 
use class name;    





